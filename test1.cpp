#include<bits/stdc++.h>
using namespace std;
int main(){
    
    #ifndef ONLINE_JUDGE

    freopen("input.txt","r", stdin);
    freopen("output.txt", "w", stdout);

    #endif
    
    // obtaining the list
    int n;
    cin >> n;

    int a;
    cin >> a;

    if(a==n)
        cout<<"first if"<<endl;
    else if(a==1)
        cout<<"second if"<<endl;


}